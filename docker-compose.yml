services:
  pxl:
    build: .
    container_name: pxl
    restart: unless-stopped
    ports:
      - "127.0.0.1:${PXL_MGMT_PORT:-3000}:${PXL_MGMT_PORT:-3000}"
      - "${PXL_TRACKER_PORT:-3001}:${PXL_TRACKER_PORT:-3001}"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
